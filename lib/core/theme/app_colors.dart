import 'package:flutter/material.dart';

class AppColors {
  // Calm, trustworthy palette (soft blue/teal + warm neutral)
  static const primary = Color(0xFF2F80ED);
  static const primarySoft = Color(0xFFBFD8FF);

  static const accent = Color(0xFF2BB3A3);

  static const background = Color(0xFFF7FAFF);
  static const surface = Color(0xFFFFFFFF);

  static const text = Color(0xFF0F172A);
  static const textMuted = Color(0xFF64748B);

  static const border = Color(0xFFE2E8F0);
}
